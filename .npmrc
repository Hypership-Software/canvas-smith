# Workday Canvas Kit declares strict React peer ranges; npm workspaces hoist fine
# with legacy peer resolution. This keeps `npm install` deterministic across the
# @workday/* + @emotion/* dependency graph.
legacy-peer-deps=true
